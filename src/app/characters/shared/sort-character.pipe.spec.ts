import { SortCharacterPipe } from './sort-character.pipe';

describe('SortCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
